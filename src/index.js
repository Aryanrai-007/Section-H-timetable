import React from "react";
import ReactDOM from "react-dom/client";
import SectionHApp from "./SectionHApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SectionHApp />
  </React.StrictMode>
);
